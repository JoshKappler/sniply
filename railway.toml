[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node scripts/init-data.mjs && PORT=${PORT:-3000} node .next/standalone/server.js"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
